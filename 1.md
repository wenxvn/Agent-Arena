
# Tyty macOS 客户端系统代理异常分析报告

**客户端：** Tyty
**版本：** 4.8.17
**系统：** macOS / MacBook Air
**代理内核：** Mihomo
**故障网络：** 手机热点 Wi-Fi
**故障类型：** Tyty 显示“系统代理”已开启，但 macOS 系统代理实际未启用

## 一、问题描述

Mac 连接不同 Wi-Fi 网络时，Tyty 的行为存在异常。

在实验室校园 Wi-Fi 环境下，开启 Tyty 后可以正常访问 Google、Grok、Gemini 等网站。

切换至手机热点后：

* Mac 本身可以正常联网；
* Tyty 可以正常启动；
* Tyty 节点显示正常；
* Tyty UI 中“系统代理”显示为开启；
* 但 Chrome 无法访问 Google、Grok、Gemini；
* Chrome 最终报 `ERR_CONNECTION_TIMED_OUT`。

最初怀疑手机热点、IPv6、DNS、MTU、运营商网络或代理节点异常，但经过逐项测试后均未发现相应故障。

---

## 二、网络基础测试

手机热点下 IPv4、IPv6 均可以正常通信。

IPv4：

```text
ping 1.1.1.1

4 packets transmitted
4 packets received
0.0% packet loss
```

IPv6：

```text
ping6 2606:4700:4700::1111

4 packets transmitted
4 packets received
0.0% packet loss
```

因此手机热点本身存在正常 IPv4/IPv6 Internet connectivity。

另外曾将 macOS：

```text
配置 IPv6：自动
```

临时改为：

```text
配置 IPv6：仅本地链接
```

故障没有发生变化。

因此 IPv6 不是此次故障的主要原因，测试后已恢复为“自动”。

---

## 三、Tyty/Mihomo 内核运行正常

检查进程发现 Tyty 4.8.17 使用 Mihomo：

```text
/Users/.../Tyty.app/Contents/Resources/sidecar/mihomo
```

Mihomo 正常运行并监听：

```text
127.0.0.1:9674
127.0.0.1:9675
127.0.0.1:9676
```

同时观察到 Mihomo 存在正常 ESTABLISHED 外部 TCP 连接，例如：

```text
mihomo → 120.53.53.53:443  ESTABLISHED
mihomo → 223.5.5.5:443     ESTABLISHED
```

因此 Mihomo 进程本身并未停止工作。

---

## 四、直接指定 Tyty 本地代理测试成功

这是定位问题最关键的测试。

在手机热点环境、Chrome 无法访问 Google 的情况下，手动通过 Tyty/Mihomo 的 `127.0.0.1:9674` 代理访问：

```bash
curl -x http://127.0.0.1:9674 -I -v --connect-timeout 10 https://www.google.com
```

结果：

```text
Connected to 127.0.0.1 port 9674

CONNECT www.google.com:443 HTTP/1.1

HTTP/1.1 200 Connection established

TLS handshake completed

SSL certificate verify ok

HTTP/2 200
```

完整 TLS 1.3 握手成功，并最终收到：

```text
HTTP/2 200
server: gws
```

`127.0.0.1:9676` 同样测试成功：

```text
HTTP/1.1 200 Connection established
TLS handshake successful
HTTP/2 200
```

因此可以确认：

```text
Mac
 ↓
手机热点
 ↓
Tyty/Mihomo
 ↓
代理节点
 ↓
Google
```

**整条代理链路可以正常工作。**

也就是说，故障并非代理节点不可用，也并非手机热点无法连接 Tyty 的代理服务器。

---

## 五、发现 Tyty UI 状态与 macOS 实际系统代理状态不一致

故障发生时，Tyty UI 中：

```text
系统代理：开启
```

显示蓝色启用状态。

但是运行：

```bash
scutil --proxy
```

macOS 返回：

```text
HTTPEnable : 0
HTTPSEnable : 0
SOCKSEnable : 0
```

即：

**Tyty UI 显示“系统代理已开启”，但 macOS 实际 HTTP/HTTPS/SOCKS 系统代理全部处于关闭状态。**

同时 IPv4 路由仍然直接使用：

```text
default → 10.120.85.19 → en0
```

Chrome 因此没有通过 Tyty 的本地代理访问目标网站。

---

## 六、手动写入 macOS 系统代理后故障立即消失

首先确认 macOS Wi-Fi Network Service：

```bash
networksetup -listallnetworkservices
```

结果：

```text
Thunderbolt Bridge
Wi-Fi
```

随后没有修改 Tyty 节点、DNS、手机热点或其他网络条件，仅手动执行：

```bash
networksetup -setwebproxy "Wi-Fi" 127.0.0.1 9674
networksetup -setsecurewebproxy "Wi-Fi" 127.0.0.1 9674
networksetup -setwebproxystate "Wi-Fi" on
networksetup -setsecurewebproxystate "Wi-Fi" on
```

再次运行：

```bash
scutil --proxy
```

macOS 立即变为：

```text
HTTPEnable : 1
HTTPPort : 9674
HTTPProxy : 127.0.0.1

HTTPSEnable : 1
HTTPSPort : 9674
HTTPSProxy : 127.0.0.1
```

随后**没有进行其他修改，Chrome 立即恢复访问 Grok、Gemini、Google。**

因此形成了非常明确的 A/B 对照：

```text
Tyty UI：系统代理 ON
macOS：HTTPEnable = 0
→ Chrome 无法访问国外网站 ❌


手动 networksetup：
HTTPEnable = 1
HTTPSEnable = 1
127.0.0.1:9674
→ Chrome 立即正常访问 ✅
```

---

## 七、故障定位

根据以上实验，目前故障最可能发生于：

**Tyty macOS 客户端的“系统代理”控制/状态同步层。**

Mihomo 本身以及代理节点可以正常工作。

疑似流程为：

```text
Tyty UI
“系统代理 ON”
       ↓
Tyty 应修改 macOS Network Service Proxy
       ↓
       ❌ 疑似未成功执行/状态未同步
       ↓
macOS HTTPEnable = 0
macOS HTTPSEnable = 0
       ↓
Chrome 未进入 Mihomo
       ↓
目标网站直连
       ↓
ERR_CONNECTION_TIMED_OUT
```

手动通过 `networksetup` 写入完全相同的本地代理后立即恢复，因此具有较强的可复现性。

---

## 八、排除项

目前测试结果可以排除或大幅降低以下因素的可能性：

* **Tyty 节点故障：** 手动通过 `127.0.0.1:9674` 可成功访问 Google。
* **Mihomo 内核故障：** Mihomo 正常监听并能建立外部连接。
* **手机热点完全阻断代理：** Mihomo 通过热点成功访问 Google。
* **Google/Grok/Gemini 本身故障：** 经 Mihomo 代理立即正常。
* **IPv6 导致：** 切换至“仅本地链接”后故障不变。
* **普通 HTTPS 整体故障：** 非代理网站 HTTPS 可正常建立 TLS。
* **DNS 为主要原因：** 通过代理可完成域名访问和 TLS。
* **Chrome 本身故障：** macOS 系统代理被正确写入后，同一 Chrome 立即恢复。

---

## 九、希望客服/开发人员重点检查

建议开发人员检查 Tyty 4.8.17 macOS 版本：

1. “系统代理”开关 ON 时，是否实际调用 macOS Network Configuration / `networksetup` 等机制修改当前活动 Network Service；
2. 从校园 Wi-Fi 切换到手机热点等网络变化后，是否需要重新应用系统代理；
3. UI 中“系统代理 ON”状态是否仅来自 Tyty 自身保存的状态，而没有读取 macOS 当前真实 Proxy State；
4. Wi-Fi 网络切换后是否存在 UI 状态与 macOS `scutil --proxy` 状态不同步的问题；
5. 是否存在 macOS 新版本兼容性或权限问题；
6. 为什么 UI 显示系统代理开启时，`scutil --proxy` 仍返回：

```text
HTTPEnable : 0
HTTPSEnable : 0
SOCKSEnable : 0
```

7. 建议客户端在开启系统代理后读取系统实际状态进行验证。如果写入失败，应提示用户，而不是继续显示“已开启”。

---

## 十、临时解决办法

故障发生后，手动执行：

```bash
networksetup -setwebproxy "Wi-Fi" 127.0.0.1 9674
networksetup -setsecurewebproxy "Wi-Fi" 127.0.0.1 9674
networksetup -setwebproxystate "Wi-Fi" on
networksetup -setsecurewebproxystate "Wi-Fi" on
```

可以立即恢复 Tyty 代理功能。

停止使用 Tyty 前需要执行：

```bash
networksetup -setwebproxystate "Wi-Fi" off
networksetup -setsecurewebproxystate "Wi-Fi" off
```

否则 macOS 仍会将 HTTP/HTTPS 流量发送至 `127.0.0.1:9674`；Tyty/Mihomo 退出后该端口无人监听，会导致网页无法访问。

---

### 结论

**现有证据更支持 Tyty 4.8.17 的 macOS“系统代理”写入或状态同步异常，而不是用户网络配置错误。**

最有力的复现证据是：

> Tyty 显示“系统代理 ON”时，`scutil --proxy` 显示 HTTP/HTTPS Proxy 均为 OFF，Chrome 无法访问；手动将同一个 Tyty `127.0.0.1:9674` 写入 macOS 系统代理后，在其他条件完全不变的情况下，Chrome 立即恢复正常访问。

因此建议 Tyty 开发人员重点排查 **系统代理开关 → macOS 当前 Wi-Fi Network Service Proxy Configuration** 这一环节。

---
