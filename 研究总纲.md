# Agent Arena 研究总纲

## 文档定位

这是 Agent Arena 的日常研究摘要。新对话和 workflow 默认参考本文件，避免每次读取完整的 `总纲.md`。`总纲.md` 保留完整的产品构想、世界细节、实验想法和长期路线，需要深入追溯时再阅读。

技术决策以 `docs/specs/` 为准，稳定模块边界以 `docs/architecture.md` 为准，开发进度以 `docs/scope/scope.md` 为准，工程工作流以 `AGENTS.md` 为准。

## 核心研究问题

Agent Arena 研究不同 Agent 架构如何影响长程任务表现。

Agent 在一个确定性、部分可观测的世界中，只能通过 Observation 了解事实，通过 Action 调用工具，并依据 Environment 的反馈继续决策。研究重点不是让模型玩游戏，而是比较 Memory、Planning 和 Reflection 是否改善成功率、效率、可靠性和失败恢复能力。

## 当前阶段

架构决策已经确认，采用 Python 3.13、uv、Pydantic v2、Typer、OpenAI Python SDK、pytest、Ruff、mypy 和 GitHub Actions。第一版是本地分层单体，只提供 CLI 和本地 JSON、CSV 文件，不提供 Web API、用户系统、数据库、Docker 或复杂 UI。

当前没有业务源代码。下一步是建立 Python 项目骨架，然后设计并实现确定性的 Spaceship Escape 环境。

## 第一版研究闭环

第一版只做一个可重复闭环：

1. 一个 Spaceship Escape 世界。
2. 六个公开工具，`look`、`move`、`inspect`、`pickup`、`use`、`read_terminal`。
3. Environment 保存完整 WorldState，Agent 只能接收 Observation。
4. 一个 ReactAgent，使用短决策说明和结构化 Action。
5. 一个受步数限制的 Episode Runner。
6. 每局一个 JSON Episode Trace。
7. 一组固定的规则测试和基础指标。
8. CLI 运行入口，默认使用 Fake provider，真实百炼调用必须显式指定。

第一版不做 MemoryAgent、PlanningAgent、ReflectionAgent、Streamlit、自动生成世界和多 Agent 编排。它们属于后续阶段，不是当前脚手架的隐含任务。

## 稳定契约

```text
Goal -> Agent -> Action -> Environment -> Observation -> Agent
                         |
                         v
                   Episode Trace -> Evaluation
```

Environment 决定事实，Agent 只决定意图。`Environment.step(action)` 是唯一的环境执行入口。所有模型输出在执行前通过同一种 Action schema 校验。Agent 不得接收完整 WorldState，也不得直接修改环境。

每局 Trace 至少记录 episode id、world version、seed、step、allowlisted Observation、短 `decision_reason`、Action、工具结果、状态、token 使用量和延迟。Trace、日志和 prompt 不得包含密钥、原始请求、原始响应或完整思维链。

## 研究变量

后续比较应保持同一世界、同一模型、同一提示词版本、同一步数预算、同一工具集合和同一初始状态。一次实验只改变一个 Agent 架构变量。

建议的版本顺序：

| 版本 | 研究对象 | 阶段 |
|---|---|---|
| A0 | ReactAgent | Release 1 |
| A1 | ReactAgent 加规则驱动结构化记忆 | Release 2 |
| A2 | MemoryAgent 加高层计划 | Deferred |
| A3 | PlanningAgent 加条件触发的反思 | Deferred |

核心指标包括成功率、步数、工具调用数、非法输出数、重复失败动作数、token 使用量、延迟和失败类型。Progress milestone 只能用于评估和调试，不能直接暴露给 Agent。

## 世界原则

第一世界是六个房间的 Spaceship Escape。世界规则必须由 Python 确定性执行，模型不能判断或修改事实。世界配置使用版本化 JSON，规则可以先写在 Python 中，不为追求纯配置驱动而过度工程化。

环境必须能够被人只使用公开工具手动通关。先测试无电不能读终端、物品获取、工具使用条件、反应堆维修、授权码获取、逃生授权和非法动作处理，再接入真实模型。

## 分阶段路线

### Foundation

完成 `src/agent_arena` 包、`pyproject.toml`、`uv.lock`、Typer CLI、配置加载、Fake provider、Ruff、mypy、pytest 和 GitHub Actions。

### Release 1

完成 Spaceship Escape 环境、环境规则测试、ReactAgent 与 Agent Loop、Episode Trace 和终止控制。

### Release 2

完成规则驱动的 MemoryAgent、benchmark 命令和 JSON、CSV 指标输出。

### Release 3

在 CLI、Trace 和 benchmark 稳定后，再增加 Streamlit 展示界面。

### Deferred

在有可重复基线后再研究 PlanningAgent、ReflectionAgent、多世界、多模型和自动失败分类。

## 研究边界

不为了展示而提前引入 LangChain、CrewAI、LangGraph、RAG、向量数据库、MCP、多 Agent、数据库、Web API、账号系统或云部署。每个新功能必须能说明研究变量、评价指标和对照组，否则先不加入。

## 完整参考

需要查看完整世界地图、谜题依赖、Memory 设计、Planning 和 Reflection 触发条件、UI 草图、README 叙事和长期方向时，阅读根目录的 `总纲.md`。
